/** 周视图网格布局常量（左侧节次列 + 7 天课程列的绝对定位布局）。 */
export const PERIOD_HEIGHT = 52; // 每个节次行的高度（px）
export const DAY_HEADER_HEIGHT = 30; // 表头行（周一…周日 / 左上角占位）高度（px）
export const TIME_COL_WIDTH = 30; // 左侧节次列宽度（px），需容纳节次号 + 起始时间（HH:MM）
export const COURSE_GAP = 2; // 相邻课程卡片之间的间隔（px）
